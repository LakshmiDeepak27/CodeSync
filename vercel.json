{
  "framework": "vite",
  "buildCommand": "npm run build --workspace=codesync-client",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
